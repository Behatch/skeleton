default:
  context:
    parameters:
      base_url: 'http://localhost'
      browser: chrome
      sahi:
        host: localhost
      filesystem:
        #root should never end up with /.
        root: "/home/myuser/Workspace/myproject"
