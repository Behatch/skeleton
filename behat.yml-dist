default:
  context:
    parameters:
      base_url: 'http://localhost/BehatCH'
      browser: chrome
      sahi:
        host: localhost
      filesystem:
        #root should never end up with /.
        root: "/home/myuser/workspace/BehatCH"
